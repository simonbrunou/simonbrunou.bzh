# node
node_modules/
npm-debug.log*
yarn-error.log
pnpm-debug.log*

# env
.env
.env.*
!.env.example

# claude code — per-clone overrides + runtime state
.claude/settings.local.json
.claude/scheduled_tasks*.lock

# superpowers brainstorming mockups (Visual Companion session data)
.superpowers/

# graphify per-clone runtime state (interpreter path, scan root)
graphify-out/.graphify_python
graphify-out/.graphify_root

# misc
.DS_Store
*.log
